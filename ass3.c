#include<stdio.h>
#include<stdlib.h>
struct node{
	int prn;
	char name[30];
	struct node *next;
};

void main()
{
	struct node *head1,*head2,*q1,*q2,*tmp;
	int pr,name,n1,n2;
	printf("Enter the number of members in div A");
	scanf("%d",&n1);
	while(n1>0)
	{
		
	}
}
